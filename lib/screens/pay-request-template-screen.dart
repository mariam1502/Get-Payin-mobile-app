import 'package:flutter/material.dart';
import '../widgets/drawer.dart';

class PayRequestTemplateScreen extends StatelessWidget {
  const PayRequestTemplateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Pay Request Template Screen')),
    );
  }
}

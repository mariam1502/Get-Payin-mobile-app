import 'package:flutter/material.dart';
import '../widgets/drawer.dart';

class ExpensesScreem extends StatelessWidget {
  const ExpensesScreem({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Expenses Screen')));
  }
}
